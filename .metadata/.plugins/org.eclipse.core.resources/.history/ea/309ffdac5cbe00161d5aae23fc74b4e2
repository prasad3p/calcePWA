package calePWA;
import java.io.FileInputStream;
import java.io.InputStream;
import org.apache.poi.hssf.usermodel.HSSFSheet;
import org.apache.poi.hssf.usermodel.HSSFWorkbook;
import dataStructures.Component;

public class FIndNodes {

	private static String COMPFILEPATH=System.getProperty("user.home") +"\\Desktop\\calecPWA\\comps.xlsx";
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		String myID="";
		String partName="";
		Component comp = new Component("", null, 0, 0, 0, "");
		comp=getComponentDetails(comp,myID,partName,COMPFILEPATH);
		
		
		
	}

	private static Component getComponentDetails(Component comp, String myID, String partName, String fileName) {
		// TODO Auto-generated method stub
		String cellContent = "Total";
		int rownr=0, colnr = 10;
		
		InputStream input = new FileInputStream(fileName);
		HSSFWorkbook wb = new HSSFWorkbook(input);
		HSSFSheet sheet = wb.getSheetAt(0);
		
		//rownr = findRow(sheet, cellContent);

		return comp;
	}

}
